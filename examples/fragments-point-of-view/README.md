# Fragments reconstruction case

Internal case record. This directory is not the public project showcase.

Retained evidence

- Exact full-slide generation and clean-plate edit prompts under `prompts/`.
- Accepted target, edited plate, original crops, OpenCV overlay, and final render under `assets/`.
- Semantic map, measured scene, reconstruction contract, and constructor scene under `data/`.
- Editable PowerPoint under `deliverables/`.

Mechanical inventory

- 21 native text boxes.
- 2 native cubic paths.
- 10 raster objects, including 7 byte-identical original crops.
- OpenCV used for pixel evidence. SAM was not used.

The collage, paper texture, intrinsic lettering, and handwriting remain raster. Presentation text and independent paths are editable. Run `python verify.py` after sealing the curated snapshot.
